// {{ generated_by }}
package {{ enum.module|jpackage}};

{% if enum.doc %}
/** {{ enum.doc }} */
{% endif %}
public enum {{ enum|jname }} {
{{ "    " }}{% for value in enum.values -%}
    {{ value.name }}{% if not loop.last and loop.length > 1 %}, {% endif %}
    {%- endfor %};

    public static final io.pdef.descriptors.EnumDescriptor<{{ enum|jname }}> DESCRIPTOR =
            io.pdef.descriptors.EnumDescriptor.of({{ enum|jname }}.class);
}

