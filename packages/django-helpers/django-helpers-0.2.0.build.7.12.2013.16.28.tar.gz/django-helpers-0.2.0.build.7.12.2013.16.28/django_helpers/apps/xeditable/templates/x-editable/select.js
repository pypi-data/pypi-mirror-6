$.dj.xEditable("#" + '{{ field_id }}', "#" + '{{ field_id }}', '{{ save_url }}', {
    type : 'select',
    pk : '{{ pk }}',
    name : '{{ name }}',
    placement : 'top',
    title : '{{ title }}',
    source : '{{ source }}'
});