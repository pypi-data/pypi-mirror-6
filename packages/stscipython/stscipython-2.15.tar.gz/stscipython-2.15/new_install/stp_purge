#!/bin/sh

. new_install/stp_tools

err=false

while read line
do
	eval `echo $line | split_line `
	echo XXXXXXXX
	echo pkg $pkg

	if rm -rf $pkg
	then
		:
	else
		err=true
	fi
done

if $err
then
	echo ERROR in checkout
	exit 1
fi
