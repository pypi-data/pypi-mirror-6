# starter pavefile

vars:
    user: ubuntu

main:
    targets:
        - 192.168.2.1               # a list/string of hosts/groups
    user: %user
    sudo: True

# passwords:                        # use only if not using keys
#   - ask password ssh Password     # default pwd for fabric

tasks:
    - packages:
        if-platform: Debian
        install: sysvbanner

    - packages:
        if-platform: Redhat
        install: banner

    - banner "Hello!"               # world
