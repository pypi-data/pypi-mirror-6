from distutils.core import setup

setup(
    name = 'divisor-1.0.0s',
    version = '1.0.0',
    py_modules = ['divisor'],
    author = 'group2',
    author_email = 'jsh001111@daum.com',
    url = '-',
    description = 'Seek divisor',
    )
