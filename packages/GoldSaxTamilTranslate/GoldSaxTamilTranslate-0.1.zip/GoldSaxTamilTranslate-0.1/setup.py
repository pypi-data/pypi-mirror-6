from distutils.core import setup
setup(
        name = 'GoldSaxTamilTranslate',
        packages = ['GoldSaxTamilTranslate'], # this must be the same as the name above
        version = '0.1',
        description = 'To enable easy translation for python applications to use this package on from English to Tamil, including a transliteration.',
        author = 'Vance King Saxbe. A',
        author_email = 'vsaxbe@yahoo.com',
        url = '',   # use the URL to the github repo
        download_url = '', # I'll explain this in a second
        keywords = ['translation', 'transliteration', 'Tamil', 'english'], # arbitrary keywords
        classifiers = [],
    )
