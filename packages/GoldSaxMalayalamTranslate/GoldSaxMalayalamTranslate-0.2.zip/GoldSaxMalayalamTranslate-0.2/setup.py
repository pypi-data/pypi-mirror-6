from distutils.core import setup
setup(
        name = 'GoldSaxMalayalamTranslate',
        packages = ['GoldSaxMalayalamTranslate'], # this must be the same as the name above
        version = '0.2',
        description = 'To enable easy translation for python applications to use this package on from English to Malayalam, including a transliteration.',
        author = 'Vance King Saxbe. A',
        author_email = 'vsaxbe@yahoo.com',
        url = '',   # use the URL to the github repo
        download_url = '', # I'll explain this in a second
        keywords = ['translation', 'transliteration', 'malayalam', 'english'], # arbitrary keywords
        classifiers = [],
    )
