#!/usr/bin/python
import sys

import timetracker

if __name__ == '__main__':
    if len(sys.argv) == 1:
        print 'What did you just do? :)'
        note = raw_input()
    else:
        note = ' '.join(sys.argv[1:])
    timetracker.log(note)

