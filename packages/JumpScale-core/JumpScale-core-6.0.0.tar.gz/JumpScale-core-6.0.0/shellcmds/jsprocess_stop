#!/usr/bin/env python
from JumpScale import j
from JumpScale.baselib import cmdutils

j.application.start("jsprocess_stop")

opt = cmdutils.getProcess()
j.tools.startupmanager.stopProcess(opt.domain, opt.name)

j.application.stop()
