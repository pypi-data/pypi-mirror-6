from math import sqrt

xcount= 10
ycount = xcount
blocksize = 60

size(xcount*blocksize, ycount*blocksize)
speed(20)
background(0,0,1)


def dist(x1,y1,x2,y2):
	return sqrt((x2-x1)**2+(y2-y1)**2)

def draw():
	for i in range(xcount):
		for j in range(ycount):
			distance = 1.0-dist((i+0.5)*blocksize,(j+0.5)*blocksize,MOUSEX,MOUSEY)/(blocksize*(xcount+3))
			fill(0.7+random(0.3),distance,0)
			#rect(i*blocksize,j*blocksize,blocksize,blocksize)
			#oval((i+0.5-distance/2)*blocksize,(j+0.5-distance/2)*blocksize,distance*blocksize,distance*blocksize)
			star((i+0.5)*blocksize,(j+0.57)*blocksize,5,distance*blocksize*0.5,distance*blocksize*0.25)
